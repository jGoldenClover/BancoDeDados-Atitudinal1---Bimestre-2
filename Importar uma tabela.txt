	CREATE TABLE classroms (
  id SERIAL PRIMARY KEY,
  curso VARCHAR(30) NOT NULL,
  local VARCHAR(20) NOT NULL UNIQUE,
  number_of_students INTEGER NOT NULL
)

--criada a tabela classroms agora vamos "importala"

--apague o codigo e digite

SELECT * FROM classroms ;

-- SELECT "selecione" * "tudo" FROM "De" (nome da tabela)

--se você mudar o * para outro elemento/coluna da tablea, você importará as informações daquela coluna

-- ex: SELECT cursos FROM classroms 

--mostrará somente os curso cadastrados na tabela